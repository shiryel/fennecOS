[binds]
# Using with oguri:
<Shift+W> = exec cp -f "$imv_current_file" ~/.cache/wallpaper

# Using only sway background:
# <Shift+W> = exec cp -f "$imv_current_file" ~/.cache/wallpaper && swaymsg output "*" background ~/.cache/wallpaper fill
